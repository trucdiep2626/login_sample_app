import 'package:flutter/material.dart';
import 'package:login_sample_app/presentation/theme/export.dart';

enum LoadedType { start, finish }

enum SnackBarType { done, error, warning }

enum NetworkMethod { get, post, delete, patch, put }

enum UserType { passenger, tourGuide }
